CONTINUE Wave 2 NOW. Stop exploring — you already know the Wave 1 codebase. Design is LOCKED (DESIGN.md). Function over fluff. Multi-week free social + venue management product.

IMPLEMENT working features immediately (schema → migrate → actions → pages → seed → docs):

1. DMs: 1:1 threads; list + send; entry from Me/profile
2. Join-request queue for venue groups (request → owner approve/deny) + existing invites
3. Owner noticeboard templates: quick post Rules / Gate codes / Updates (official)
4. Seed 2 more system global groups; document adding globals
5. Notifications: mark-as-read + unread badge on Me/header
6. Featured flag on Venue (boolean); badge on discover; NO payments

Migrate DB, keep demos working. Update PRODUCT.md (wave2 done), STATUS.md. Write WAVE2.md with how Chris tries it. Screenshots optional if time.

Do NOT restyle. Do NOT merge to main. Prefer write/edit tools over more reads. Ship working code.
