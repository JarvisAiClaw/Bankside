import Link from "next/link";
import { register } from "../actions";
import { Notice } from "@/components/ui";
export default async function Register({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const { error } = await searchParams; return <div className="shell max-w-xl py-14"><div className="card p-7 sm:p-10"><h1 className="text-3xl font-black">Join Bankside</h1><p className="mt-2 text-black/60">Free forever. Choose the account that fits.</p><Notice message={error}/><form action={register} className="mt-7 space-y-5"><div><label className="label" htmlFor="name">Your name</label><input className="field" id="name" name="name" required minLength={2}/></div><div><label className="label" htmlFor="email">Email</label><input className="field" id="email" name="email" type="email" required/></div><div><label className="label" htmlFor="password">Password</label><input className="field" id="password" name="password" type="password" minLength={8} required/><p className="mt-1 text-xs text-black/45">At least 8 characters</p></div><fieldset><legend className="label">Account type</legend><div className="grid gap-3 sm:grid-cols-2"><label className="cursor-pointer rounded-xl border bg-white p-4"><input type="radio" name="accountType" value="angler" defaultChecked className="mr-2 accent-brand"/><strong>Angler</strong><span className="mt-1 block text-sm text-black/55">Join groups and share posts.</span></label><label className="cursor-pointer rounded-xl border bg-white p-4"><input type="radio" name="accountType" value="venue_owner" className="mr-2 accent-brand"/><strong>Venue owner</strong><span className="mt-1 block text-sm text-black/55">Create and manage venue groups.</span></label></div></fieldset><button className="btn w-full">Create free account</button></form><p className="mt-6 text-center text-sm text-black/55">Already a member? <Link className="font-semibold text-brand" href="/login">Log in</Link></p></div></div> }
