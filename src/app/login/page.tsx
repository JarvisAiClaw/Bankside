import Link from "next/link"; import { login } from "../actions"; import { Notice } from "@/components/ui";
export default async function Login({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const { error } = await searchParams; return <div className="shell max-w-lg py-16"><div className="card p-7 sm:p-10"><h1 className="text-3xl font-black">Welcome back</h1><p className="mt-2 text-black/60">Sign in to see what’s happening.</p><Notice message={error}/><form action={login} className="mt-7 space-y-5"><div><label className="label" htmlFor="email">Email</label><input className="field" id="email" name="email" type="email" required/></div><div><label className="label" htmlFor="password">Password</label><input className="field" id="password" name="password" type="password" required/></div><button className="btn w-full">Log in</button></form><p className="mt-6 text-center text-sm text-black/55">New here? <Link className="font-semibold text-brand" href="/register">Join free</Link></p></div></div> }
