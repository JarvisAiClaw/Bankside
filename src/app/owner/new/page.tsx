import { redirect } from "next/navigation"; import { Role } from "@prisma/client"; import { getCurrentUser } from "@/lib/current-user"; import { createVenue } from "@/app/actions"; import { Notice } from "@/components/ui";
export default async function NewVenue({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const user = await getCurrentUser(); if (!user) redirect("/login"); if (user.role !== Role.VENUE_OWNER && user.role !== Role.ADMIN) redirect("/discover"); const { error } = await searchParams; return <div className="shell max-w-2xl py-12"><div className="card p-8"><h1 className="text-3xl font-black">Register a venue</h1><p className="mt-2 text-black/60">This automatically creates its official group and owner admin area.</p><Notice message={error}/><form action={createVenue} className="mt-7 space-y-5"><div><label className="label" htmlFor="name">Venue name</label><input className="field" id="name" name="name" required/></div><div><label className="label" htmlFor="location">Location</label><input className="field" id="location" name="location" required placeholder="Town, county"/></div><div><label className="label" htmlFor="description">Description</label><textarea className="field min-h-36" id="description" name="description" minLength={10} required placeholder="Tell anglers about the water, species and atmosphere."/></div><button className="btn">Register venue and create group</button></form></div></div> }
